[workspace]
resolver = "2"
members = [
  "contracts/*",
  "contracts/review-system",
]
# educational-nft is excluded: depends on stellar-* v0.4.0 packages (OpenZeppelin)
# which are incompatible with soroban-sdk v25. Pending upstream release.
exclude = [
  "contracts/educational-nft",
]

[workspace.dependencies]
soroban-sdk = { version = "25.3.0", features = ["alloc"] }

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

# For more information about this profile see https://soroban.stellar.org/docs/basic-tutorials/logging#cargotoml-profile
[profile.release-with-logs]
inherits = "release"
debug-assertions = true

[profile.test]
opt-level = 0
debug = true
incremental = true
codegen-units = 256
